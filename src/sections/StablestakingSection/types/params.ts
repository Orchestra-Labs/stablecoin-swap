export type StablestakingParams = {
  params: {
    reward_rate: string;
    unbonding_duration: string;
    supported_tokens: string[];
    reward_epoch_identifier: string;
    unbonding_epoch_identifier: string;
    max_staking_amount: string;
  };
};
